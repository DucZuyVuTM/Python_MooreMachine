# Python_MooreMachine

## How to check coverage?

Windows:

```bash
coverage run main.py
coverage report -m
```

Linux:

```bash
python3 -m coverage run main.py
python3 -m coverage report -m
```
